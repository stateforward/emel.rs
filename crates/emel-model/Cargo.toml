[package]
name = "emel-model"
description = "Model architectures and lifecycle loading for emel."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
# Internal cross-crate proof surface. This deliberately exports only the actor
# and typed events; the production tensor cutover remains deferred.
model-tensor-proof = []

[dependencies]
sml = { workspace = true }

[dev-dependencies]
allocation-counter = "=0.8.1"

[[example]]
name = "tensor_parity"
required-features = ["model-tensor-proof"]

[lints]
workspace = true
